#include <iostream>
#include <vector>

using namespace std;

    void mergeSort(vector<int>& arr, int n){

    }

int main(){

    int n;
    vector<int> arr(n);

    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }

    mergeSort(arr,n);

}